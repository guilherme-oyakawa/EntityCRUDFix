@model IEnumerable<NewGameStore.Models.Client>

@{
    ViewBag.Title = "Index";
}

<h2>Clients</h2>

@using (Html.BeginForm("Index", "Clients", FormMethod.Get))
{
    <p>
        Find by name: @Html.TextBox("SearchString", ViewBag.CurrentFilter as string)
        <input type="submit" value="Search" class="btn btn-info" />
    </p>
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstMidName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BirthDate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstMidName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BirthDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ClientID }, new { @class = "btn btn-default" })
                @Html.ActionLink("Activate", "Delete", new { id = item.ClientID }, new { @class = "btn btn-success" })
            </td>
        </tr>
    }

</table>
<td>
    @Html.ActionLink("Previous Page", "", new { page = ViewBag.CurrentPage - 1 }) |
    @Html.ActionLink("Next Page", "", new { page = ViewBag.CurrentPage + 1 })
</td>