@model IEnumerable<NewGameStore.Models.Genre>

@{
    ViewBag.Title = "Index";
}

<h2>Genres</h2>

<p>
    @Html.ActionLink("New Genre", "Create", null, new { @class = "btn btn-success" })
    @Html.ActionLink("View Publishers", "../Publishers", null, new { @class = "btn btn-default"})
    @Html.ActionLink("Back to Games", "../Games/Index", null, new { @class = "btn btn-default" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("View Games", "Games", new { id = item.GenreID }, new { @class = "btn btn-info" })
            @Html.ActionLink("Delete", "Delete", new { id=item.GenreID }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
